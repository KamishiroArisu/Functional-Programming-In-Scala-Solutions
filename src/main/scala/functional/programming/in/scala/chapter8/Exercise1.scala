package functional.programming.in.scala.chapter8

object Exercise1 {
    //1. Reversing a list and summing it should give the same result as summing the original, nonreversed list.
    //2. The sum should be head multiply length if list is nonempty and all elements of the list are the same value
    //3. The sum should be 0 if list is empty
    //...
}
